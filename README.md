# Домашнее задание 8

Необходимо формально проверить, что тестируемый модуль полностью соответствует спецификации

На входе нам, как верификаторам, доступна документация на модуль (`doc/adapter.md`), в которой описана теория работы модуля (которую мы, как верификаторы, должны понимать, чтобы осознанно и корректно тестировать модуль), а также описаны свойства схемы, которые являются ключевыми (например, эти свойства просил заказчик разработки данного модуля)

Для формальной верификации вам предлагается перевести данные свойства с русского языка на язык SVA, сделать это необходимо в файле `src/adapter_sva.sv`, который не содержит ничего, кроме будущих шести SVA (`h0`-`h5`). В идеальных условиях, мы,
как верификаторы, не должны видеть RTL-код, поэтому весь RTL-код модуля вынесен в отдельный файл: нам доступна документация на интерфейс, и поэтому все SVA мы должны описывать, используя известные имена портов — никаких внутренних сигналов модуля мы не можем использовать, так как микроархитектура модуля для нас "недоступна"

В файле `src/adapter_sva.sv` уже содержится пример для свойства h0, который поможет разобраться с синтаксисом

Сначала нужно добиться того, что `main` тест проходит полностью успешно, а дальше проверять с помощью остальных `h` тестов, что при ("скрытой" от вас) поломке RTL в известном месте каждый соответствующий тест ожидаемо падает (значит ваш SVA детектирует невыполнение свойства модулем). Например, для свойства `h0` написан корректный SVA, на тесте `main` это свойство будет PASS, так как RTL написан корректно и наш SVA проверяет данное свойство; но в специальном тесте `h0` в RTL вносится ошибка в месте выдачи `valid_o`, и наш корректный SVA в тесте `h0` должен стать FAIL, так как RTL больше не обеспечивает требуемое свойство.

## Структура репозитория

1. `doc/`
    - документация на тестируемый модуль
2. `src/`
    - исходный файл для написания SVA (он встраивается в RTL-код модуля для тестирования)
3. `tests/`
    - тесты, проверяющие написанные свойства
    - подробнее в секции "Запуск тестов"

## Спецификация модуля

Читай `adapter.md` в `/doc`

## Запуск тестов

```
make run TARGET=test_name
```

или сразу все вместе

```
make run_all
```

Архитектура проверки:
1. тест `main` должен полностью проходить успешно
    - RTL для его проверки эталонный
    - все проверки должны быть PASS
2. далее на каждое свойство существует отдельный тест (`h0`-`h5`)
    - каждый тест использует искаженный RTL,
в котором сломана именно та (и только та) логика, которая проверяется соответствующим SVA
    - проверка этого утверждения должна быть FAIL
    - предыдущие утверждения (так как утверждения после упавшего не проверяются) должны быть PASS
3. таким образом проверяется, что все утверждения написаны корректно
    - они PASS на эталонном RTL
    - они FAIL в случае, если в RTL допущен баг (т.е. RTL искажен)

### Доступные тесты

| Название | Строка запуска | Описание | Ожидаемый результат |
|----------|----------|----------|----------|
| main     | make run TARGET=main | Проверяет все шесть утверждений, все должны выполняться | PASS |
| h0       | make run TARGET=h0   | Проверяет утверждение h0; RTL изменен так, что верное утверждение должно падать | FAIL h0 |
| h1       | make run TARGET=h1   | Проверяет утверждение h1; RTL изменен так, что верное утверждение должно падать | FAIL h1 |
| h2       | make run TARGET=h2   | Проверяет утверждение h2; RTL изменен так, что верное утверждение должно падать | FAIL h2 |
| h3       | make run TARGET=h3   | Проверяет утверждение h3; RTL изменен так, что верное утверждение должно падать | FAIL h3 |
| h4       | make run TARGET=h4   | Проверяет утверждение h4; RTL изменен так, что верное утверждение должно падать | FAIL h4 | 
| h5       | make run TARGET=h5   | Проверяет утверждение h5; RTL изменен так, что верное утверждение должно падать | FAIL h5 |

### Артифакты

Каждый тест складывает свои выходные данные в директорию `build`, в свою поддиректорию.
Если тест упал, то он выведет в консоль имя упавшего свойства, а также положит `vcd` контрпримера,
на котором свойство не выполняется (в поддиректории `engine_0`) — открыв ее, можно посмотреть,
какая последовательность входных сигналов приводит к нарушению свойства, и произвести необходимую
доработку SVA